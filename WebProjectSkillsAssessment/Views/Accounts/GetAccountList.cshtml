@using WebProjectSkillsAssessment.Domain.Entities
@model List<Account>
<style>
    .top-right {
        top: 0;
        right: 0;
    }
</style>
<div class="jumbotron jumbotron-fluid">
    <p>
        <table class=table>
            <thead class="table-primary">
                <tr>
                    <th>@Html.DisplayName("Code")</th>
                    <th>@Html.DisplayName("AccountNumber")</th>
                    <th>@Html.DisplayName("Action")</th>
                </tr>
            </thead>
            <tbody>
                 
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem =>item.Code)</td>
                        <td>@Html.DisplayFor(modelItem =>item.AccountNumber)</td>
                        <td>
                            <a asp-controller="/Accounts" asp-action="GetAccountDetailsByAccountNumber" asp-route-AccountNumber="@item.AccountNumber" class="btn bg-gradient-primary mt-2 me-2">Edit</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </p>
</div>